[package]
name = "rustashop-template-default"
description = "Default shop template assets (HTML/SCSS). Path package for hosts."
version = "0.1.0"
edition = "2021"
license = "OSL-3.0"
publish = false

[lib]
path = "lib.rs"

# Brand crop masters

These PNG files are the **source of truth** for rustashop brand art (user crops).

- Keep this directory. Do not delete it after publishing named copies.
- Do not run transparency / flood-fill scripts on these files.
- Named copies and size declensions live under `docs/brand/` (copy + resize only).

See `docs/brand/README.md` for the UUID → name map.
